# Task Plan: Issue #184 — 合流停止の実環境診断と安全なログ

## Metadata
- Owner: Codex
- Branch: `codex/rendezvous-hazard-observability`
- Related issue: #184
- Last updated (UTC): 2026-09-21

## 1. 目的
- 実ワールドの呼び寄せ2回が `rendezvous_hazard_blocked` で停止した内訳を、取得できる範囲の読み取り専用観測で切り分ける。
- 次回の安全停止で、判定箇所と固定理由を、話者名・座標・生のワールド情報をログに残さず判別できるようにする。

## 2. 非目標
- Botの移動、採掘、チャット送信、サーバー再起動、設定変更、デプロイ。
- 地形に合わせた経路選択や危険判定の緩和。実際の停止内訳が確定する前に安全条件を変更しない。
- 現在のワールド観測を、失敗時点の状態と同一視すること。

## 3. 対象範囲
- 読み取り専用の既存Node/Paper観測経路、Node合流の危険判定ログ、回帰テスト、最小の契約文書。
- 外部の `followPlayer` 応答コードとPython/Bridge契約は維持する。

## 4. マイルストーン
| ID | 内容 | 状態 | 確認点 |
| --- | --- | --- | --- |
| M1 | 実環境を読み取り専用で再観測 | Done | Bot周辺は観測。対象と中継地点は取得できず、過去の停止理由は未確定 |
| M2 | 安全停止の固定分類ログとテスト | Done | 段階・理由の固定分類、実値非露出、外部応答維持をNodeテストで確認 |
| M3 | 公開安全性、CI、レビュー、PR配送 | Done | コード変更のCI全3件成功、レビュー指摘なし。計画記録を追加した最終HEADも確認する |

## 5. 優先度付き小タスク
- [x] P0: 既存の読み取り経路で現在の地形・対象・Mobを観測し、観測不能はそのまま記録する。
- [x] P0: `rendezvous_hazard_blocked` の判定箇所と固定理由を1回の停止から特定できるログを追加する。
- [x] P0: 階段の高低差を含むfakeで、停止分類と機微情報非露出をテストする。
- [x] P1: Node全テスト・ビルド、文書/公開安全性、最新HEAD CIとレビューを確認する。

## 6. 受け入れ条件
- [x] 実環境の観測事実、コード上の仮説、未確認事項を分離する。
- [x] 新規ログが停止箇所と固定理由を示し、話者名、チャット全文、精密座標、生のblock/entity、認証情報を含まない。
- [x] 外部エラーとfail-closedの移動判断が従来どおりである。
- [x] テスト・ビルド・CI・レビュー結果と、実ゲーム移動未実施の理由がPRへ残る。

## 7. 検証コマンド
- [x] `bash scripts/run-node-bot.sh test`（22 files / 194 tests）
- [x] `bash scripts/run-node-bot.sh build`
- [x] `git diff --check`
- [x] コード変更HEADのCIとPRレビュー・threadを確認。計画記録を追加した最終HEADは別途再確認

## 8. 基本スモークテスト
- 手順: fake Botの足場・液体・Mob・階段を切り替えて合流を呼び出す。
- 期待結果: 従来と同じ安全停止コードで止まり、固定段階・理由のみを1回記録する。

## 9. 再開コマンド
- `git switch codex/rendezvous-hazard-observability`
- `git status --short --branch`
- `gh issue view 184 --repo stillshore-chirp/mc-bot-egent-practice-1`

## 10. 既知 Blocker
- 失敗時点のblock/entity snapshotは現行ログにない。現在の観測値だけでは過去の真因を断定できない。
- PaperのBridgeプラグインは現在ロードされておらず、対象の位置照会と中継地点の観測ができない。Jar生成がPaper起動より後で、起動時コピーが行われなかった構成が観測された。設定整合と再起動は今回行わない。
- 実ワールド移動は今回の許可範囲外。

## 11. Rollback
- Nodeの診断ログ変更を責務単位でrevertする。ワールド・永続データには触れない。

## 12. ステータスログ
- 2026-09-21: 最新mainから作業ブランチを作成。Issue #184へ追加範囲を記録。実ワールドでは2回とも同一の固定安全エラーを返し、移動開始前の停止が有力。具体的な危険理由は未観測。
- 2026-09-21: 現在のBotは接続中。周辺には空洞候補がある一方、液体と敵対Mobは観測されなかった。対象と中継地点は不明で、失敗時点から世界状態も変わり得るため、直接原因の確定には使わない。Bridgeプラグイン未ロードは別の観測制約として記録。固定分類ログを追加しNodeテスト・ビルドを通した。実ワールドでの新ログ検証は再起動を伴うため未実施。
- 2026-09-21: コード変更のPRを非Draftで作成。コード変更HEADのCIはNode・Python・Bridgeの3件成功。PRの自動レビュー、コメント、未解決threadは確認時点でなく、反証レビューにもP0/P1なし。計画記録のみ追加した後、最終HEADのCIとレビューを再確認する。

## 13. 停止時の最終状態
- 最終状態: Done（実ワールドでの再現は許可範囲外、過去の停止内訳は未確定）
- 停止理由: なし。実ワールドへの変更操作は行わない。
- 再開条件: 実ワールドへ反映しての検証が必要な場合、対象サービスの再起動・設定変更の別途許可。
- 次の最短アクション: 計画記録をコミットし、最終HEADのCIとPRレビューを確認する。
