# Task Plan: Issue #184 — 階段地形での合流中継地点

## Metadata
- Owner: Codex
- Branch: `codex/issue-184-waypoint-floor`
- Related issue: #184
- Last updated (UTC): 2026-09-21

## 1. 目的
- 階段ブロック名を空気と誤判定する処理を修正し、同じ中継地点で一段の高低差を観測できる場合に安全な合流を開始できるようにする。
- 空洞、液体、敵対Mob、観測不能では移動前に停止する既存の安全性を維持する。

## 2. 非目標
- 実Minecraftワールドでの移動、採掘、サーバーやBotの再起動、設定変更、デプロイ。
- 未観測地形への楽観的な経路決定、安全判定の無条件緩和、外部コマンド契約の変更。
- 経路全体の列走査、斜め経路の網羅、途中に丘がある同高度経路、多段階段の一般的な探索。これらは今回の局所修正後も別途検証が必要。

## 3. 対象範囲
- `node-bot/runtime/navigationController.ts`、その単体テスト、必要な仕様文書。
- Python/Bridgeの契約とチャット応答文言は維持する。

## 4. マイルストーン
| ID | 内容 | 状態 | 確認点 |
| --- | --- | --- | --- |
| M1 | 中継地点の安全条件と回帰ケースを固定 | Done | 実ログとコード仮説を分離し、広い走査案のP1を反証して範囲を縮小 |
| M2 | 足場に沿った中継地点選択を実装 | Done | fake Botで一段の上り下りを開始し、危険時は停止 |
| M3 | 検証、公開安全性、PR・CI・レビュー | Done | PR #187の初回CI全件成功、review/threadなし。最終HEADは再確認する |

## 5. 優先度付き小タスク
- [x] P0: 高低差のある安全な階段で従来停止する回帰テストを作る。
- [x] P0: `stairs` に含まれる `air` 文字列を空気判定しない厳密名の回帰テストを作る。
- [x] P0: 観測済み足場だけを候補として使い、危険・不明なら停止する。
- [x] P0: 液体、空洞、敵対Mob、未観測、移動期限の既存挙動を回帰検証する。
- [x] P1: Nodeテスト・ビルド、文書、公開安全性、CI、レビューを確認する。

## 6. 受け入れ条件
- [x] fake Botで安全な一段差では最初の区間移動を開始できる。
- [x] 階段ブロック名を空気と誤認せず、空気3種と未知ブロックは安全扱いしない。
- [x] fake Botで支えがない地点、液体・危険ブロック、近傍敵対Mob、観測不能では移動しない。
- [x] 外部応答コード、距離・期限・再試行上限、ログの個人情報非露出を維持する。
- [x] 実ログで確定した停止箇所と、実地形が安全だったかどうかの未確定性をPRに記載する。

## 7. 検証コマンド
- [x] `bash scripts/run-node-bot.sh test`（22 files / 215 tests passed）
- [x] `bash scripts/run-node-bot.sh build`
- [x] `git diff --check`
- [x] PR #187の初回HEADのCI 3件成功、review/threadなし、mergeableを確認。最終HEADはpush後に再確認する。

## 8. 基本スモークテスト
- 手順: fake Botで一段上がる安全な足場、空洞、液体、敵対Mob、観測不能を切り替えて合流を試す。
- 期待結果: 安全な足場のみ最初のgotoへ進み、危険・不明はgotoせず固定エラーを返す。

## 9. 再開コマンド
- `git switch codex/issue-184-waypoint-floor`
- `git status --short --branch`
- `gh issue view 184 --repo stillshore-chirp/mc-bot-egent-practice-1`

## 10. 既知 Blocker
- 失敗時の中継地点と足元ブロックの実値は未取得。ログは停止箇所と固定理由を示すが、真の空洞と階段誤判定の識別は未完了。
- 実ワールド移動による検証は今回の許可範囲外。
- 広い列走査案は斜め方向の中間列、到着許容半径、端数座標で安全性の反証が残ったため採用しない。

## 11. Rollback
- Nodeの中継地点選択変更を責務単位でrevertする。ワールド状態と永続データには触れない。

## 12. UI/UX 反証レビュー
- 対象はゲーム内チャットの合流導線。画面レイアウト・文言は変更しない。
- 状態は受理・確認中、移動中、成功、危険停止、経路なし、観測不能を確認。失敗時は移動せず、既存の回復案を一度だけ通知する。
- 初見では受理メッセージと最終結果を区別できる。熟練者の反復操作、キーボード・フォーカス・コントラストは変更の影響外。ゲーム内実表示と読み上げは未確認。
- 残る懸念は最大210秒の移動中に進捗通知がないこと、危険理由が抽象的なこと、実Pathfinderと実ゲームでの到達が未検証なこと。これらは本修正の完了根拠にしない。

## 13. ステータスログ
- 2026-09-21: 新しい実ログで waypoint / unsupported_floor / gotoStarted=false を確認。最新mainから作業ブランチを作り、Issue #184に追加の範囲と未確定事項を記録。
- 2026-09-21: 追加のコード確認で、従来の `name.includes('air')` が `stairs` にも一致することを確認。実ログとの整合はあるが失敗地点の実ブロック名は未取得として、厳密な空気判定を追加範囲に含めた。
- 2026-09-21: 広い列走査案は反証レビューで未観測列とPathfinder到着半径の問題が判明。配送範囲を階段名の厳密判定と同一中継地点の一段の補正へ縮小し、危険・未観測は停止を維持する。
- 2026-09-21: Node 22 files / 215 tests、build、diff checkを確認。二段候補、Pathfinder開始セル、泡の柱の境界テストを追加。実Minecraftの地形・移動結果は未検証。
- 2026-09-21: PR #187を作成。初回HEADのCIはPython・Node・Bridgeの3件成功。review/threadはこの時点でなし。公開文書・PR本文にsecret、プレイヤー名、座標、rawログを載せていないことを確認。

## 14. 停止時の最終状態
- 最終状態: Done（コード・テスト・PR配送）。実ゲームE2Eは非目標として未実施。
- 停止理由: なし。
- 再開条件: 実ゲームでの合流確認が別途許可された場合、またはPRのレビュー指摘が発生した場合。
- 次の最短アクション: この計画更新後の最新HEADでCIとreview/threadを再確認する。
